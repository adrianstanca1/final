# Copy this file to .env.local and fill in the secrets before running locally.
# When deploying to Vercel set the same variables in the project settings.

# Public client-side API key for Gemini. Required for AI powered features.
VITE_GEMINI_API_KEY=

# Backend base URL (if you have a live API). When unset, the app uses mock services.
# Example: https://api.example.com
VITE_BACKEND_URL=

# Supabase project URL and the PUBLIC anon key for client-side authentication (Google OAuth, etc.).
# NEVER put database passwords or the service_role key in client env vars.
# Your URL format is: https://<project-ref>.supabase.co
# For host db.zpbuvuxpfemldsknerew.supabase.co, the project URL is:
#   https://zpbuvuxpfemldsknerew.supabase.co
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional: default tenant code for multi-tenant flows (can be set at runtime too).
VITE_DEFAULT_TENANT_CODE=
