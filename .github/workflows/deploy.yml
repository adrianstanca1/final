name : Disabled - legacy deploy

                      on : workflow_dispatch

                               jobs : noop : runs -
                                             on : ubuntu - latest steps : -run : echo "This workflow is disabled. Use vercel-deploy.yml."
