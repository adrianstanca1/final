# Copy to .env.local or server envs as appropriate
# Client-exposed variables must use VITE_ prefix
VITE_API_BASE=

# Server-only secrets (do NOT prefix with VITE_)
GEMINI_API_KEY=
# Optional: URL for the Python preprocess service if used from server code
MM_PREPROCESS_URL=http://localhost:8010/mm/preprocess
