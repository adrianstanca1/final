# Webspace Host Production Environment Configuration
# ASAgents Platform - Webspace Host Deployment

# ===========================================
# SERVER CONFIGURATION
# ===========================================

# Server Settings
NODE_ENV=production
PORT=3000
HOST=0.0.0.0

# API Configuration
API_VERSION=v1
API_PREFIX=/api
CORS_ORIGIN=https://access-5018479851.webspace-host.com
CORS_CREDENTIALS=true

# ===========================================
# DATABASE CONFIGURATION (Update with your DB details)
# ===========================================

# Database Settings - UPDATE THESE WITH YOUR ACTUAL DATABASE CREDENTIALS
DB_HOST=localhost
DB_PORT=3306
DB_NAME=asagents_production
DB_USER=a1038987
DB_PASSWORD=your-database-password
DB_SSL=false
DB_CONNECTION_LIMIT=10
DB_TIMEOUT=30000

# Database Pool Configuration
DB_POOL_MIN=2
DB_POOL_MAX=10
DB_POOL_IDLE_TIMEOUT=30000
DB_POOL_ACQUIRE_TIMEOUT=60000

# ===========================================
# AUTHENTICATION & SECURITY
# ===========================================

# JWT Configuration - CHANGE THESE SECRETS IN PRODUCTION
JWT_SECRET=your-super-secure-jwt-secret-change-this-webspace-production
JWT_EXPIRES_IN=24h
JWT_REFRESH_EXPIRES_IN=7d
JWT_ALGORITHM=HS256

# Session Configuration
SESSION_SECRET=your-super-secure-session-secret-change-this-webspace-production
SESSION_COOKIE_SECURE=true
SESSION_COOKIE_HTTP_ONLY=true
SESSION_COOKIE_SAME_SITE=strict
SESSION_MAX_AGE=86400000

# OAuth Configuration
GOOGLE_CLIENT_ID=892922789443-d6laifg42fjsuvkjpbja7989ulrfdqqu.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GITHUB_CLIENT_ID=Iv23lihOkwvRyu8n7WdY
GITHUB_CLIENT_SECRET=your-github-client-secret
FACEBOOK_APP_ID=your-facebook-app-id
FACEBOOK_APP_SECRET=your-facebook-app-secret

# OAuth Redirect URLs
GOOGLE_REDIRECT_URL=https://access-5018479851.webspace-host.com/api/auth/google/callback
GITHUB_REDIRECT_URL=https://access-5018479851.webspace-host.com/api/auth/github/callback
FACEBOOK_REDIRECT_URL=https://access-5018479851.webspace-host.com/api/auth/facebook/callback

# ===========================================
# AI SERVICES CONFIGURATION
# ===========================================

# Gemini AI Configuration
GEMINI_API_KEY=AIzaSyC4BTpQS0_ZdZsOl0c3beb344hr3xZEVy8
GEMINI_MODEL=gemini-2.0-flash-001
GEMINI_MAX_TOKENS=8192
GEMINI_TEMPERATURE=0.7

# AI Processing Configuration
AI_PROCESSING_TIMEOUT=30000
AI_MAX_CONCURRENT_REQUESTS=5
AI_RETRY_ATTEMPTS=3
AI_RETRY_DELAY=1000

# ===========================================
# FILE STORAGE CONFIGURATION
# ===========================================

# File Upload Configuration
UPLOAD_MAX_SIZE=52428800
UPLOAD_ALLOWED_TYPES=image/*,audio/*,video/*,application/pdf,text/*
UPLOAD_DESTINATION=/uploads/
UPLOAD_TEMP_DIR=/tmp/

# Local Storage (for webspace hosting)
CLOUD_STORAGE_PROVIDER=local
STATIC_FILES_URL=https://access-5018479851.webspace-host.com/uploads

# ===========================================
# LOGGING & MONITORING
# ===========================================

# Logging Configuration
LOG_LEVEL=info
LOG_FORMAT=combined
LOG_FILE=/logs/app.log
LOG_MAX_SIZE=10m
LOG_MAX_FILES=3

# Monitoring & Health Checks
HEALTH_CHECK_INTERVAL=60000
METRICS_ENABLED=true
PERFORMANCE_MONITORING=false

# Error Reporting
ERROR_REPORTING_ENABLED=true
SENTRY_DSN=your-sentry-dsn

# ===========================================
# PERFORMANCE & CACHING
# ===========================================

# Simple file-based caching (no Redis needed)
CACHE_TYPE=file
CACHE_DIR=/tmp/cache
CACHE_TTL=3600

# Rate Limiting
RATE_LIMIT_WINDOW=900000
RATE_LIMIT_MAX_REQUESTS=100
RATE_LIMIT_SKIP_SUCCESSFUL=false

# Compression & Optimization
COMPRESSION_ENABLED=true
COMPRESSION_LEVEL=6
STATIC_CACHE_MAX_AGE=31536000

# ===========================================
# EMAIL CONFIGURATION (Optional)
# ===========================================

# SMTP Configuration - Update with your email provider
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=true
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-email-password
EMAIL_FROM=noreply@access-5018479851.webspace-host.com

# ===========================================
# SECURITY HEADERS & PROTECTION
# ===========================================

# Security Configuration
HELMET_ENABLED=true
CSRF_PROTECTION=true
XSS_PROTECTION=true
CONTENT_SECURITY_POLICY=true
HSTS_ENABLED=false
HSTS_MAX_AGE=31536000

# API Security
API_KEY_REQUIRED=false
API_RATE_LIMITING=true
API_REQUEST_VALIDATION=true

# ===========================================
# WEBSPACE HOST SPECIFIC CONFIGURATION
# ===========================================

# Domain Configuration
DOMAIN=access-5018479851.webspace-host.com
FRONTEND_URL=https://access-5018479851.webspace-host.com
BACKEND_URL=https://access-5018479851.webspace-host.com/api

# SSL Configuration (handled by hosting provider)
SSL_ENABLED=true
FORCE_HTTPS=true

# Process Management (simple for shared hosting)
PM2_INSTANCES=1
PM2_EXEC_MODE=fork
PM2_MAX_MEMORY_RESTART=512M

# Shared Hosting Optimizations
MAX_CONCURRENT_CONNECTIONS=50
REQUEST_TIMEOUT=30000
KEEP_ALIVE_TIMEOUT=5000

# File Paths (webspace host structure)
WEB_ROOT=/html
NODE_ROOT=/nodejs
UPLOADS_ROOT=/uploads
LOGS_ROOT=/logs
